## Agent skills

### Issue tracker

Issues live in GitHub Issues for `Og128/Keptang`, managed via the `gh` CLI. See `docs/agents/issue-tracker.md`.

### Domain docs

Single-context: root `CONTEXT.md` + `docs/adr/`. See `docs/agents/domain.md`.
